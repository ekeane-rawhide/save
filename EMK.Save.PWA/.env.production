# Filled in at build time for a production deploy — Vite bakes these into the client bundle,
# so they're not secrets, just the real public URLs for your deployed API.
VITE_API_URL=https://api.yourdomain.com/api
VITE_HUB_URL=https://api.yourdomain.com/hubs/save
VITE_VAPID_PUBLIC_KEY=
